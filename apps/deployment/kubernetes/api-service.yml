apiVersion: v1
kind: Service
metadata:
  name: open-tombola-service
  labels:
    app: open-tombola-service
spec:
  ports:
    - port: 80
      targetPort: 3333
      protocol: TCP
      name: http
  selector:
    app: open-tombola-api

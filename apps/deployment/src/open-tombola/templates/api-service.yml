apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: {{ .Release.Name }}-service
  namespace: {{ .Values.namespace }} 
spec:
  ports:
    - port: 80
      targetPort: 3333
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-api

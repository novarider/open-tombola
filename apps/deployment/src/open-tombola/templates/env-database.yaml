apiVersion: v1
kind: Secret
metadata:
  name: env-database
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  {{- range $key, $value := .Files.Get ".env.database" | split "\n" }}
  {{- if and $value (not (hasPrefix "#" $value)) }}
  {{- $parts := split "=" $value }}
  {{- if eq (len $parts) 2 }}
  {{ trim $parts._0 }}: {{ trim $parts._1 | quote }}
  {{- end }}
  {{- end }}
  {{- end }} 